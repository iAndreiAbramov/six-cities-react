import React from 'react';

export const HomePageMap: React.FC = () => {
    return (
        <div className="cities__right-section">
            <section className="cities__map map"></section>
        </div>
    );
};
